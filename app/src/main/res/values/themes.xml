<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Estiloya" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Colores claros -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/text</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Personalización adicional -->
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/background</item>

        <item name="popupBackgroundColor">#FFFFFF</item>
        
        <!-- Configuraciones para filtros -->
        <item name="android:textColor">@color/filter_text</item>
        <item name="android:textColorHint">@color/filter_hint</item>
        <item name="android:colorBackground">@color/filter_background</item>
    </style>
</resources>
